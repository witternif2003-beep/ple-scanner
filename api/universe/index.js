const{Redis}=require("@upstash/redis");const U=require("../cron/universe.json");const B=5;module.exports=async(r,s)=>{s.setHeader("Access-Control-Allow-Origin","*");s.setHeader("Content-Type","application/json");let b=0,a=0,m=0;try{const x=Redis.fromEnv();b=Number(await x.get("ple:batch_idx")||0);a=Number(await x.get("ple:api_calls")||0);m=Number(await x.get("ple:matches_today")||0)}catch(e){}s.status(200).json({total:U.length,batch:b,totalBatches:Math.ceil(U.length/B),batchSize:B,apiCalls:a,matchesToday:m})};
